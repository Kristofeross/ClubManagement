@model Account;

<div>
    <h1>Konto Użytkownika - @Model.AccountName</h1>
    <p><a asp-controller="home" asp-action="index">Powrót</a></p>
</div>
@if (TempData["Alert"] != null)
{
    <div class="alert alert-danger ">
        @TempData["Alert"]
    </div>
}

<div>
    @* <a class="nav-link text-dark" asp-controller="UserManagement" asp-action="EditAccount" asp-route-id="@Model.Id" asp-route-type="accountName">Edytuj nazwę konta</a> *@
    <a class="nav-link text-dark" asp-controller="UserManagement" asp-action="EditAccount" asp-route-id="@Model.Id" asp-route-type="email">Edytuj email</a>
    <a class="nav-link text-dark" asp-controller="UserManagement" asp-action="EditAccount" asp-route-id="@Model.Id" asp-route-type="password">Edytuj hasło</a>
</div>